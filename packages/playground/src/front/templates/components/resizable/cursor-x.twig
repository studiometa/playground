{#
/**
 * @file
 *   Resizable Cursor X
 *
 * @description
 *   Create an horizontal resizable handler, which is displaid vertically.
 *
 * @params {array} $attr       Attributes for the root element.
 * @params {array} $components Additional components for the root element.
 */
#}

{% set attributes =
  merge_html_attributes(
    attr,
    {},
    {
      data_component: ['ResizableCursor']|merge(components ?? [])|join(' '),
      data_option_axis: 'x',
      class: 'z-10 flex-grow flex flex-col items-center justify-center gap-1 w-2 h-full bg-zinc-100 dark:bg-zinc-700 cursor-col-resize'
    }
  )
%}

<div {{ html_attributes(attributes) }}>
  <div class="w-1 h-1 rounded-full bg-zinc-300 dark:bg-zinc-900"></div>
  <div class="w-1 h-1 rounded-full bg-zinc-300 dark:bg-zinc-900"></div>
  <div class="w-1 h-1 rounded-full bg-zinc-300 dark:bg-zinc-900"></div>
</div>
