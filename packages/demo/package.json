{
  "name": "@studiometa/playground-demo",
  "version": "0.2.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "meta dev",
    "watch": "meta watch",
    "build": "meta build",
    "prepreview": "npm run build",
    "preview": "esbuild --serve --servedir=dist"
  },
  "dependencies": {
    "@studiometa/playground": "*"
  },
  "devDependencies": {
    "esbuild": "0.24.0"
  }
}
